---
# VLAN NAD for BGP peering (router ↔ external gateway)
# VLAN 100: 169.254.100.x/24 + fd00:100::x/64
apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: vlan-nad
  namespace: e2e-ns-a
spec:
  config: |
    {
      "cniVersion": "0.3.1",
      "type": "vlan",
      "master": "eth0",
      "vlanId": 100,
      "ipam": {
        "type": "static",
        "addresses": [
          { "address": "169.254.100.1/24" },
          { "address": "fd00:100::1/64" }
        ]
      }
    }
---
# Macvlan NAD for application network (SLLBR ↔ app Pods)
# Dual-stack via whereabouts IPAM
apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: app-net
  namespace: e2e-ns-a
spec:
  config: |
    {
      "cniVersion": "1.0.0",
      "name": "app-net-a",
      "plugins": [
        {
          "type": "macvlan",
          "master": "eth0",
          "mode": "bridge",
          "ipam": {
            "type": "whereabouts",
            "ipRanges": [
              { "range": "169.111.100.0/24" },
              { "range": "fd00:111::0/120" }
            ]
          }
        }
      ]
    }
